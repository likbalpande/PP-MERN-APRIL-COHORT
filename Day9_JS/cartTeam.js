const validateCart = () => {
    // true
    if(false){
        setTimeout(() => {
            console.log('validating cart...');
        }, 2000);
    }
    else{
        setTimeout(() => {
            console.log('validation failed!');
        }, 2000);
    }
}

export default validateCart;